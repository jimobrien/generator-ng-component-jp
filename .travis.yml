language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ng-component-jp' ]; then cd .. && eval "mv $currentfolder generator-ng-component-jp" && cd generator-ng-component-jp; fi

